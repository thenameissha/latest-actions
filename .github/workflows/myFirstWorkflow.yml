name: myFirstWorkflow
on: push
jobs:
  job1:
    name: Job1
    runs-on: ubuntu-latest
    steps:
      - name: Running command in job
        run: echo "hi"  
      - name: check OS release
        run: cat /etc/os-release
  job2:
    name: Job2
    runs-on: ubuntu-latest
    steps: 
    - run: hostname
      name: Checking host name
    - run: |
        echo "hi from Sha"
      name: printing my hi
    


    
